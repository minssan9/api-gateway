---
spring:
  profiles:
    group:
      "local": "local, default"
---
spring:
  profiles:
    group:
      "develop": "develop, default"
---
spring:
  profiles:
    group:
      "release": "release, default"
